SGDS v1.03 Type Scale Correction

Upload this entire folder as the contents of /sgds.

This build keeps the current three-column mockup and contained header, but reduces the overall type scale so the display feels calmer and less oversized on a large lobby screen.

Rich's Rule: complete package, no patch files.
